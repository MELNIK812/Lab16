# Работа с массивами в C

## 📋 Задание к лаб. 16
1. Генерация массивов случайных чисел
2. Вывод массивов на экран
3. Создание результирующего массива D, содержащего максимальные значения из трех исходных массивов A, B, C

## 📁 Структура проекта
lab16/

├── source.c

└── README.md


## 🔧 Реализованные функции

### `double* generate_array(int size)`
- **Назначение**: Генерация массива случайных чисел
- **Параметры**: 
  - `size` - размер генерируемого массива
- **Возвращает**: указатель на созданный массив

### `void print_array(double* arr, int size, char name)`
- **Назначение**: Вывод массива на экран
- **Параметры**:
  - `arr` - указатель на массив
  - `size` - размер массива
  - `name` - символьное имя массива для вывода
- **Возвращает**: void

### `double* create_array_d(double* a, int size_a, double* b, int size_b, double* c, int size_c, int* size_d)`
- **Назначение**: Создание массива D из максимальных элементов массивов A, B, C
- **Параметры**:
  - `a, b, c` - указатели на исходные массивы
  - `size_a, size_b, size_c` - размеры исходных массивов
  - `size_d` - указатель для возврата размера созданного массива
- **Возвращает**: указатель на созданный массив D
./arrays
